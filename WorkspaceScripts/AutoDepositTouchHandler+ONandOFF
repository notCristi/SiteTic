local ReplicatedStorage = game:GetService("ReplicatedStorage")
local GamepassToggle = require(ReplicatedStorage.Modules.GamepassToggle)

GamepassToggle.Setup{
    part = script.Parent,
    passName = "AUTO_DEPOSIT",
    attribute = "AutoDepositActive"
}
